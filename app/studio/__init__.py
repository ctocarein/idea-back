# app/studio — génération d'assets de marque (logo, kit) façon Looka/Brandmark.
# Moteur : SVG génératif hybride (l'IA choisit dans un vocabulaire curé ; on compose
# le SVG déterministe). Vectoriel, éditable, sans dépendance externe.
