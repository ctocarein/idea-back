# Revue de code quotidienne — Ideaxion backend (idea-back)

**Date :** 2026-07-11 · **Référentiel :** `docs/ARCHITECTURE_BACKEND.md` (FastAPI v2.0)
**Dépôt :** `ctocarein/idea-back` @ `main` · **Aucune modification de code effectuée** (constats seulement).

---

## Résumé exécutif

**Commits des dernières 24 h : 0.** Le dernier commit (`90332ca`, perf diagnostic) date du 2026-07-10 08:29 UTC, en dehors de la fenêtre 24 h, et a déjà été revu en détail hier (`revue-2026-07-10.md`). Aucun code n'a changé depuis la dernière exécution.

En l'absence de nouveauté, cette revue est une **revue de fond légère par rotation** : scan ciblé des modules **les moins audités** à ce jour — `notifications` (0 revue), `studio`, `academy`, `pitch` (1 revue chacun) — plus les vérifications systématiques (couches, secrets, Alembic).

| Sévérité | Nouveaux constats (24 h) |
|----------|--------------------------|
| 🔴 Critique | 0 |
| 🟠 Important | 0 |
| 🟡 Mineur | 0 |

**Le plus urgent :** rien de neuf. Les points ouverts restent **des constats déjà documentés** (idempotence du double-commit diagnostic, audit de la reprise radar-only, orchestration dans `jobs/router`, export Chromium synchrone du pitch). Ils sont rappelés ci-dessous sans être re-détaillés (rotation).

---

## Constats de la rotation du jour

Le scan des modules sous-audités **n'a révélé aucun nouvel écart** au référentiel. Détail des vérifications :

### Modules scannés cette fois — conformes

- **`notifications/`** — `router.py` passe exclusivement par `NotificationService` (`Depends(get_notification_service)`), aucune requête SQL ni `sqlalchemy` dans le router, aucune orchestration repo+audit+commit dans le handler. Séparation des couches respectée (Principes 1 & 2).
- **`studio/`** — `service.generate` appelle le LLM via `self.provider.analyze_json` (fournisseur résilient issu de `app/llm/factory`), enveloppé dans un `try/except` avec **mode dégradé** (`default_variations`) et **rate-limit** au router (`logo_generate`, 12/60 s, `fail_open=False`). Conforme aux Principes 1 et 3.
- **`academy/`** — router → service → repository propre ; `provider.complete` pour la génération de fiche, `render_fiche_pdf` (WeasyPrint, CPU-borné) en import paresseux. RAS.

### Vérifications systématiques — conformes

- **Séparation des couches (Principe 1).** Aucun router n'exécute de SQL brut ni n'importe `sqlalchemy` pour requêter, à l'exception du **health-check** `platform/router.py` (`session.execute(text("SELECT 1"))`) — **exception légitime** : sonde de liveness des dépendances, best-effort et bornée, sans repository dédié possible.
- **`core` technique (Principe 2).** Aucun métier détecté dans `app/core/` ; les clients techniques (LLM, storage, cache, mailer) restent hors des features.
- **Secrets (config).** Aucun secret en dur trouvé hors `app/core/config.py` (`pydantic-settings` / `SecretStr`).
- **Migrations Alembic.** Toutes les migrations sous `alembic/versions/` possèdent une fonction `downgrade` — réversibilité respectée.
- **Écritures de statut.** Les mutations `ReportStatus` de `reports/repository.py` (`mark_ready`, `mark_failed`) sont bien dans le repository. La seule écriture directe hors couche reste `diagnostics/handlers.py:265` — **déjà signalée hier** (voir rotation ci-dessous).

---

## Points ouverts déjà connus — rappelés, non re-détaillés (rotation)

Ces constats restent d'actualité (code inchangé) mais ont déjà été traités en détail dans des revues antérieures. Ils ne sont pas re-signalés au titre de la règle de rotation :

1. 🟠 **Idempotence du diagnostic « deux commits ».** Le rejeu de `handle_run_diagnostic` après le commit de Phase 1 peut doubler l'audit `diagnostic.scored`, le `ScoreRun` et les appels LLM. → détail complet dans `revue-2026-07-10.md`. **Non corrigé** (aucun commit depuis).
2. 🟠 **Reprise « radar-only » sans audit** + 🟡 **mutation directe `report.status` dans le handler** (`diagnostics/handlers.py:265`). → `revue-2026-07-10.md`. **Non corrigé.**
3. 🟠 **`jobs/router.py` orchestre repo + audit + commit** et instancie `JobRepository(session)` en ligne (routes `retry_job`, `list_jobs`, `job_stats`). → détaillé dans `revue-2026-06-28.md` (et rappels 06-25 → 06-30). **Non corrigé.**
4. 🟠 **Export pitch : Chromium/Playwright synchrone en requête HTTP**, sans timeout global ni passage par la file de jobs ; le router `export_pitch` ne rattrape que `ImportError` (pas les échecs/latences runtime du navigateur). → détaillé dans `revue-2026-07-05.md` (rappel 07-08). **Non corrigé.**

---

## Points conformes (synthèse)

- **Aucune régression** introduite : pas de commit sur les dernières 24 h.
- **Modules `notifications` / `studio` / `academy`** : séparation des couches, injection par `Depends`, résilience LLM et modes dégradés respectés.
- **Résilience I/O** des appels LLM héritée de `app/llm/factory` (timeout, retry, circuit breaker, fallback) sur tous les modules qui l'utilisent (`studio`, `academy`, `pitch`, `diagnostics`).
- **Secrets, Alembic, `core` technique** : conformes.

---

*Revue de fond par rotation — aucun changement de code depuis `revue-2026-07-10.md`. Prochaine exécution : reprendre la revue ligne à ligne dès qu'un commit apparaît dans la fenêtre 24 h ; sinon poursuivre la rotation sur `mentors`, `documents`, `reports`, `iam`.*
