# Revue de code quotidienne — Ideaxion backend (idea-back)

**Date :** 2026-07-12 · **Référentiel :** `docs/ARCHITECTURE_BACKEND.md` (FastAPI v2.0)
**Dépôt :** `ctocarein/idea-back` @ `main` · **Aucune modification de code effectuée** (constats seulement).

---

## Résumé exécutif

**Commits des dernières 24 h : 0.** Le dernier commit (`db35137`, *fix(diagnostic) : scoring tolérant aux passes en échec (rate-limit)*) date du 2026-07-11 09:55 UTC, hors de la fenêtre 24 h. Aucun code n'a changé depuis la dernière exécution (`revue-2026-07-11.md`).

En l'absence de nouveauté, cette revue est une **revue de fond légère par rotation**, poursuivant sur les modules annoncés hier comme les prochains à auditer : **`mentors`, `documents`, `reports`, `iam`**, plus les vérifications systématiques (couches, secrets, Alembic, écritures de statut, résilience I/O).

| Sévérité | Nouveaux constats (24 h) |
|----------|--------------------------|
| 🔴 Critique | 0 |
| 🟠 Important | 0 |
| 🟡 Mineur | 0 |

**Le plus urgent :** rien de neuf. Le scan de rotation n'a fait ressortir qu'**une seule classe de problème, déjà documentée** — les appels MinIO/`storage` **synchrones bloquants dans des handlers `async`** et **sans timeout / retry / circuit breaker** côté client. Elle touche `documents`, `pitchsim` et `gdpr`, mais a déjà été détaillée (06-25, 07-03, 07-05) ; elle est donc **rappelée sans être re-détaillée** (règle de rotation), et reste **non corrigée** (aucun commit depuis).

---

## Constats de la rotation du jour

Le scan des modules ciblés **n'a révélé aucun nouvel écart** au référentiel. Détail des vérifications.

### Modules scannés cette fois — conformes

- **`mentors/`** — séparation des couches propre : `router.py` passe exclusivement par `MentorService` (injection `Depends`), aucun SQL ni `sqlalchemy` dans le router ; `repository.py` ne connaît pas le HTTP. Les mutations de statut (`app.status = APPROVED/REJECTED` l.92/113, `user.status = ACTIVE/SUSPENDED` l.138/329) vivent bien **dans le service** (couche métier), sont **gardées par une machine à états explicite** (`if app.status is not PENDING: raise BusinessRuleError`) et **auditées dans la même transaction** (`auditor.record("mentor.approved"/"mentor.rejected"/…)` puis `commit`). Conforme aux Principes 1 et 4.
- **`documents/`** — router → service → repository respecté ; le storage est **injecté** (`ObjectStorage | None`), l'API ne transporte jamais les octets (upload PUT présigné), et les chemins storage KO **dégradent proprement** (`try/except`, suppression MinIO best-effort). Seul point ouvert : appels `storage` synchrones sans `to_thread` (voir rappel de rotation ci-dessous).
- **`reports/`** — couches propres ; `render_bilan_html` est **pur** (stdlib, testable hors-ligne), le WeasyPrint bloquant reste réservé au **worker** ; `get_pdf_url` invoque MinIO **via `asyncio.to_thread`** (`service.py:123`) — **modèle correct** à généraliser. RAS.
- **`iam/`** — `router.py` propre (aucun `sqlalchemy`, aucune instanciation `Repository(...)`/`Service(...)` en ligne, injection par `Depends`) ; `service.py` audite les actions sensibles (`auditor.record`, l.72) ; aucune écriture de statut hors couche. RAS.

### Vérifications systématiques — conformes

- **Séparation des couches (Principe 1).** Aucun des routers `mentors`/`documents`/`reports`/`iam` n'exécute de SQL brut, n'importe `sqlalchemy` pour requêter, ni n'instancie un repository/service en ligne. Aucun repository ne connaît le HTTP.
- **`core` technique (Principe 2).** Pas de métier détecté dans `app/core/` ; `app/core/storage.py` reste un seam technique pur.
- **Secrets (config).** Aucun secret en dur trouvé hors `app/core/config.py` (`pydantic-settings` / `SecretStr`).
- **Migrations Alembic.** Toutes les migrations sous `alembic/versions/` possèdent une fonction `downgrade` — réversibilité respectée.
- **Écritures de statut.** Les seules mutations `.status =` hors repository sont dans `mentors/service.py` (couche métier, gardées + auditées, cf. ci-dessus) — conforme. La mutation directe `report.status` de `diagnostics/handlers.py` reste le seul écart déjà connu (voir points ouverts).

---

## Points ouverts déjà connus — rappelés, non re-détaillés (rotation)

Ces constats restent d'actualité (code inchangé) mais ont déjà été traités en détail antérieurement. Ils ne sont pas re-signalés au titre de la règle de rotation :

1. 🟠 **`storage`/MinIO sans garde-fou I/O** — le client MinIO (`app/core/storage.py`) est créé **sans timeout**, et les opérations réseau bloquantes (`stat_object`, `remove_object`, `put_bytes`, `get_bytes`, `ensure_bucket`) n'ont **ni retry, ni circuit breaker** ; seul le **mode dégradé** (`try/except`) est présent. → détaillé dans `revue-2026-06-25.md`. **Non corrigé.**
2. 🟠 **Appels `storage` synchrones dans des handlers `async` (blocage de l'event loop)** — le contrat du module (`storage.py:5` : « les appelants async doivent l'invoquer via `asyncio.to_thread` ») est respecté à certains endroits (`reports/service.py:123`, `pitchsim/service.py:205`, `diagnostics/handlers.py:211`, `platform/router.py:48`) mais **enfreint** dans `documents/service.py` (`presigned_put` l.49 → `ensure_bucket`, `stat_object` l.62, `remove_object` l.66/69/88), `pitchsim/service.py:146` (`put_bytes`) et `gdpr/service.py:102` (`remove_object`, dans une **boucle** de purge RGPD). → classe déjà relevée les 07-03 et 07-05. **Non corrigé.** *Correction : envelopper ces appels via `asyncio.to_thread` et, à terme, via `app/core/resilience` (retry + circuit breaker) avec timeout client explicite (cible ~15 s), comme `app/llm/openai_compatible.py`.*
3. 🟡 **`documents/service.py` — suppression de document non auditée** et 🟡 **`gdpr/service.py` — export de portabilité non tracé.** → `revue-2026-07-03.md` / `revue-2026-07-05.md`. **Non corrigés.**
4. 🟠 **Idempotence du diagnostic « deux commits »** + 🟠 **reprise « radar-only » sans audit** + 🟡 **mutation directe `report.status`** (`diagnostics/handlers.py`). → `revue-2026-07-10.md`. **Non corrigés.**
5. 🟠 **`jobs/router.py` orchestre repo + audit + commit** et instancie `JobRepository(session)` en ligne. → `revue-2026-06-28.md`. **Non corrigé.**
6. 🟠 **Export pitch : Chromium/Playwright synchrone en requête HTTP**, hors file de jobs. → `revue-2026-07-05.md`. **Non corrigé.**

---

## Points conformes (synthèse)

- **Aucune régression** introduite : pas de commit sur les dernières 24 h.
- **`mentors`** : machine à états sur les statuts (candidature, compte), audit systématique dans la même transaction, séparation des couches nette.
- **`documents` / `reports` / `iam`** : couches propres, injection par `Depends`, storage injecté et dégradation gracieuse ; `reports.get_pdf_url` montre le **bon patron** (`to_thread`) à généraliser.
- **Secrets, Alembic (downgrade), `core` technique** : conformes.

---

*Revue de fond par rotation — aucun changement de code depuis `revue-2026-07-11.md`. Le seul point matériel ressorti aujourd'hui (storage synchrone / sans timeout) est **déjà documenté** et rappelé au titre de la rotation. Prochaine exécution : revue ligne à ligne dès qu'un commit apparaît dans la fenêtre 24 h ; sinon poursuivre la rotation sur `opportunities`, `sharing`, `scoring`, `academy` (zones les moins récemment auditées).*
